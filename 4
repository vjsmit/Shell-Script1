## In a file, every line starts with ^ and end with $
    - we can use this to search for the word using  ^. e.g. $ grep ^root /etc/passwd
    - we can use this to search for the word using $.  e.g. $ grep bash$ /etc/passwd
